import {
    ready,
    beforeReady,
    runReadyCallbacks
} from './ready';
import {
    onDeviceListen,
    runDeviceListenCallback
} from './onDeviceListen';

/**
 * @module System
 */
export default {
    ready,
    beforeReady,
    runReadyCallbacks,
    onDeviceListen,
    runDeviceListenCallback
}