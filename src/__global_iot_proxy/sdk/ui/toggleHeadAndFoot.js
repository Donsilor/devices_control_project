/**
 * 显示/隐藏app头部和底部
 * @function toggleHeadFoot
 * @memberOf module:UI
 * @param {object} options JsBridge通信协议要求实现
 * @param {string} options.data JsBridge通信协议要求传递数据,需要JSON格式字符串
 * @param {boolean} options.data.show_head_foot 是否显示或者隐藏头部和底部:true,false
 * @example
 * HdIot.UI.toggleHeadFoot({
 *      data: JSON.stringify({
 *          show_head_foot:true
 *      })
 * })
 */

export default function (options) {
}